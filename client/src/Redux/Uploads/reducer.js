import { createSlice } from '@reduxjs/toolkit';

const uploadsSlide = createSlice({
  name: 'uploads',
  initialState: {},
  reducers: {},
  extraReducers: {}
});

export default uploadsSlide.reducer;