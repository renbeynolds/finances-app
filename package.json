{
  "name": "finances-app",
  "version": "0.1.0",
  "dependencies": {},
  "scripts": {
    "install-all": "concurrently -p name -c \"magenta,blue,yellow\" -n \"server,client,root\" \"cd server && yarn install\" \"cd client && yarn install\" \"yarn install\"",
    "start": "concurrently -p name -c \"magenta,blue\" -n \"server,client\" \"cd server && yarn start\" \"cd client && yarn start\""
  },
  "devDependencies": {
    "concurrently": "^6.4.0",
    "cypress": "^10.2.0",
    "plop": "^3.1.0",
    "prettier": "^2.4.1"
  }
}
